---
import LegalPage from '../components/LegalPage.astro';
import { client } from '../lib/sanity';
import { legalPageQuery } from '../lib/queries';

const page = (await client.fetch(legalPageQuery, { slug: 'terms' })) || {};
---

<LegalPage page={page} id="terms" />
